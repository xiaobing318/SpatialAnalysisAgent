tool_ID = "grass7_r.surf.random"
tool_name = "r.surf.random"
brief_description = "r.surf.randomr.surf.random- Produces a raster surface map of uniform random deviates with defined range."
synopsis = """r.surf.random
r.surf.random --help
r.surf.random[-i]output=name[min=float]   [max=float]   [seed=integer]   [--overwrite]  [--help]  [--verbose]  [--quiet]  [--ui]"""
flags = """-i
    Create an integer raster map
--overwrite
    Allow output files to overwrite existing files
--help
    Print usage summary
--verbose
    Verbose module output
--quiet
    Quiet module output
--ui
    Force launching GUI dialog"""
parameters = """output=name[required]
    Name for output raster map
min=float
    Minimum random value
    Default:0
max=float
    Maximum random value
    Default:100
seed=integer
    Seed for random number generator
    The same seed can be used to obtain same results or random seed can be generated by other means."""
code_example = """
from qgis.core import QgsProcessingParameterRasterDestination
from qgis.core import QgsProcessing
import processing

params = {
    'output': 'output_path',  # OUTPUT parameter
    'min': 0.0,  # Minimum random value
    'max': 100.0,  # Maximum random value
    'seed': None  # Seed for random number generator
}

processing.run(\"""grass7:r.surf.random\", params)"""